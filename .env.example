# =============================================================================
# Environment Variables for Dagster Pipelines
# =============================================================================
# Copy this file to .env and update values as needed.
#
# For local development with docker-compose.dev.yml, the default values work.
# For cluster access, update with your actual credentials.
# =============================================================================

# MinIO Configuration
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=password123

# LakeFS Configuration
LAKEFS_ENDPOINT=localhost:8000
LAKEFS_ACCESS_KEY_ID=AKIAIOSFOLKFSSAMPLES
LAKEFS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYSAMPLEKEY

# Weaviate Configuration
WEAVIATE_HOST=localhost
WEAVIATE_PORT=8080
WEAVIATE_GRPC_HOST=localhost
WEAVIATE_GRPC_PORT=50051

# NIM LLM Configuration (mock - will use fallback values locally)
NIM_ENDPOINT=http://localhost:8001
NIM_REASONING_ENDPOINT=http://localhost:8002

# NIM Embedding Configuration (mock - use_mock_fallback=True by default)
NIM_EMBEDDING_ENDPOINT=http://localhost:8003

# Safe Synthesizer Configuration (requires Kubernetes + GPU)
SAFE_SYNTH_NAMESPACE=nvidia-ai
SAFE_SYNTH_ENDPOINT=http://localhost:8004
SAFE_SYNTH_PRIORITY=batch-high
NDS_ENDPOINT=http://localhost:3000
NDS_REPO=admin/central-bank-speeches
HF_TOKEN=your_huggingface_token

# Kaggle Configuration (required for speeches pipeline)
# Get your key from: https://www.kaggle.com/settings -> API -> Create New Token
KAGGLE_USERNAME=your_kaggle_username
KAGGLE_KEY=your_kaggle_key